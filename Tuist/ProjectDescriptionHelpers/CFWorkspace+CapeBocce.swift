import CFTuist
import ProjectDescription

public extension CFWorkspace {

    static let capeBocce = CFWorkspace.default(
        name: "cb-frontend",
        xcodeVersion: Version(15, 3, 0)
    )

}
